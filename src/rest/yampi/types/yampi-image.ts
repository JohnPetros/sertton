export interface YampiImage {
  readonly large?: { readonly url: string }
  readonly medium?: { readonly url: string }
  readonly thumb?: { readonly url: string }
  readonly small?: { readonly url: string }
}
